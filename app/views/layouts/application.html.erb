<!DOCTYPE html>
<html>
  <head>
    <title>GameTestGuru</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% if user_signed_in? %>
    <header>
      <div class="header-style">
        <table class="table_lk" >
          <th class="lk1">
            <table class="label" >
              <th class="lk1"> <%= link_to "Test Guru", root_path, id: "logo" %></th>
              <th class="lk1"><%= link_to "Выйти", destroy_user_session_path, method: :delete %></th>
            </table>
          </th>
          <th class="lk1"> <p class="nav user"> <%= (current_user.first_name).gsub(/\b./, &:upcase) %>, добро пожаловать <b>(@<%= current_user.email %>)</b> в <%= link_to TestGuru, root_path %>!</p></th>
        </table>
      </div>
    </header>
  <% elsif user_signed_in? %>
    <header>
      <div class="header-style">
        <table class="table_lk" >
          <th class="lk1">
            <table class="label" >
              <th class="lk1"> <%= link_to "Test Guru", root_path, id: "logo" %></th>
              <th class="lk1"><%= link_to "Выйти", destroy_user_session_path, method: :delete %></th>
            </table>
          </th>
          <th class="lk1"> <p class="nav user"> <%= (current_user.first_name).gsub(/\b./, &:upcase) %>, добро пожаловать <b>(@<%= current_user.email %>)</b> в <%= link_to TestGuru, root_path %>!</p></th>
        </table>
      </div>
    </header>
  <% else %>
    <header>
      <div class="header-style">
        <table class="table_lk" >
          <th class="lk1">
            <table class="label" >
              <th class="lk1"> <%= link_to "Test Guru", root_path, id: "logo" %></th>
              <th class="lk1"><%= link_to "Авторизация", new_user_session_path %></th>
              <th class="lk1"><%= link_to "Регистрация", new_user_registration_path %></th>
            </table>
          </th>
          <th class="lk1"> <p class="nav user"></p></th>
        </table>
      </div>
    </header>
  <% end %>

  <%= render 'shared/flash' %>

    <%= yield %>
  </body>

  <%= render 'shared/footer' %>


</html>
