<nav class="navbar navbar-light bg-primary fixed-top", style= "width: 82%; margin: 0 auto;" data-bs-theme="dark" >

  <div class="container" >
        <style>
            .navbar-brand {
                border: none !important;
                outline: none !important;
            }

            .table_lk th {
                border: none !important;
            }
        </style>

        <div class="nav lk1_admin d-flex justify-content-between align-items-center w-100">
          <div class="nav label">
            <%= link_to "Test Guru", admin_tests_path, id: "logo", class: "navbar-brand", style: "color: black" %>
          </div>
          <div class="nav title_text fs-4 fw-bold ms-3">
            <%= 'ADMIN PANEL' %>
          </div>
          <div class="nav lk1_admin ms-auto">
            <%= button_to "Выйти", destroy_user_session_path, method: :delete, form: { class: 'd-inline' }, class: "btn btn-outline-light btn-lg", style: "padding: 0.5rem 1.5rem; border-radius: 0.2rem; text-align: center;" %>
          </div>
        </div>
      </div>
      <div class="container">
        <div class="nav lk1_admin text-end mt-2">
          <p class="nav user mb-0">
            <%= (current_user.first_name).gsub(/\b./, &:upcase) %> <b>(<%= current_user.email %>)</b>, добро пожаловать в TestGuru!
          </p>
        </div>
      </div>
</nav>
