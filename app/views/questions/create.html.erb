<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Полный список вопросов</title>
</head>
<body>
<style>

    h1 {
        font-size: 24px;
        text-transform: uppercase;
        font-style: italic;
        font-family: 'William', serif;
        margin-bottom: 0;
        margin-top: 0;
        padding-top: 34px;
        font-weight: normal;
        color: white;

    }

    h2 {
        margin-top: 0px;
        font-size: 30px;
        text-transform: uppercase;
        font-family: 'Inter', sans-serif;
        margin-bottom: 24px;
        color: white;

    }

    h3 {
        font-size: 15px;
        text-transform: uppercase;
        font-family: 'William', serif;
        margin: 5px 5px 5px 10vh;
        line-height: 18px;
        font-weight: normal;
        color: white;
        text-align: left !important;

    }

    p {
        font-family: 'Inter', sans-serif;
        text-align: center;

    }
    .rails-default-about-page {
        background-color: mediumaquamarine;
        width: 50%;
        height: auto;
        text-align: center;
        font-family: arial, sans-serif;
        margin: auto;
        overflow: auto
    }

    .field {
        clear:both;
        text-align:right;
        line-height:25px;

    }
    .main {float:left}

    label {
        float:left;
        padding-right:10px;
        padding-left: 40px;

    }



    input {
        font-family: inherit;
        font-size: inherit;
        line-height: inherit;
        margin: 0;
        font: inherit;
        color: inherit;
        padding-inline-end: 44px;
        padding-inline-start: 52px;
        text-align: center;
    }

    textarea {
        font-family: inherit;
        font-size: inherit;
        line-height: inherit;
        margin: 0;
        font: inherit;
        color: inherit;
        padding-inline-end: 44px;
        padding-inline-start: 52px;
        text-align: center;
    }

    table {
        border-collapse: collapse;
        margin: 0 auto;
        width: 98%;
        border-collapse: collapse;
        border: 0.2px solid #333030;
    }
    td, th {
        border: solid 1px #000;
        padding: 5px;
        text-align: left;
    }

    stylesheet
    input[type="text" i] {
        padding: 1px 2px;
        float: left;

    }
    input[type="submit"] {
        border-radius: 25px;
        padding-inline-start: 52px;
        line-height: inherit;
    }
    stylesheet

</style>


<div class="rails-default-about-page">
  <br> <br> <br> <br>
  <h1>Вопрос <%=@question.body %>></h1>
  <h3></h3>

  <table class="table table-striped table-sm">
    <thead>
    <tr>
      <th scope="col">Номер вопроса</th>
      <th scope="col">Вопрос</th>
      <th scope="col">Номер теста</th>
      <th scope="col">Дата обновления</th>
    </tr>
    </thead>
    <tbody>
    <%@questions.each do |question| %>
      <tr>
        <td><%=question.id%></td>
        <td><%=link_to question.body, question_path(question)%></td>
        <td><%=question.test_id%></td>
        <td><%=question.updated_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
        <td><%=button_to "Удалить", question_path(question), method: :delete %></td>
      </tr>
      </tbody>
    <%end%>
    </table>
  <br>
  <br>
</div>
<p>Сделано по ТЗ от Thinknetica: <a href = " https://thinknetica.com" target="_blank">www.thinknetica.com</a></p>
<p><a href = "about/author">Информация об авторе</a></p>
</body>
</html>
