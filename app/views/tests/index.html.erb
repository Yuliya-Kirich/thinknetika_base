<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Полный список тестов</title>
</head>
<body>

<div class="rails-default-about-page">
  <br> <br> <br> <br>
  <h1>Полный список тестов</h1>
  <h3></h3>

  <table class="table table-striped table-sm">
    <thead>
    <tr>
      <th scope="col">Номер теста</th>
      <th scope="col">Тест</th>
      <th scope="col">Дата обновления</th>
    </tr>
    </thead>
    <tbody>
    <%@test.each do |test| %>
    <tr>
      <td><%=test.id%></td>
      <td><%=link_to test.title, test_path(test), id: test.id %></td>
      <td><%=test.updated_at.strftime("%d-%m-%Y %H:%M:%S") %></td>
      <td><%=button_to "Удалить", test_path(test), method: :delete %></td>
    </tr>
      </tbody>
    <%end%>
    </table>
  <br>
  <br>
</div>
<p>Сделано по ТЗ от Thinknetica: <a href = " https://thinknetica.com" target="_blank">www.thinknetica.com</a></p>
<p><a href = "about/author">Информация об авторе</a></p>
</body>
</html>
>